<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title><%= title %></title>
</head>
<body>
    <%- include('../navbar', {}) %>
    <h1>File Details</h1>
    <table>
            <tr class="firstRow">
            <td>file name</td>
            <td>folder name</td>
            <td>file size</td>
        </tr>
        <tr>
            <td><%= file.originalName %></td>
            <td><%= file.folder.name %></td>
            <td><%= (file.size / 1024).toFixed(2 ) %>KiB (<%= file.size %> bytes)</td>
            <td><a href="/folders/<%= file.folderId %>/files/<%= file.id %>/download">Download</a></td>
        </tr>
    <table>
        <a href="/folders/<%= file.folderId %>/files">Back to Files</a>
</body>
</html>